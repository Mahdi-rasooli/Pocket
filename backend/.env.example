MONGODB_URI=your-mongodb-atlas-connection-string
JWT_SECRET=your-long-random-secret
PORT=5000
# Optional. Comma-separated list of allowed frontend origins for CORS, e.g.
# https://pocket.example.com. Leave unset for local dev (allows all origins).
CORS_ORIGIN=
